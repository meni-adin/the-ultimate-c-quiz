Replace the following blanks in the code with the same characters to turn the code valid:

```c
int main()
{
    switch(1)
    {
        case /* replace */:
            return 0;
        case /* replace */:
            return 0;
    }
}
```
