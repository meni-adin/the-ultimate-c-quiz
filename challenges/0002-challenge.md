Explain how is it possible that the following code is a valid `C` program?

```
%:include <stdio.h>
??=define macro(VAR) ??<VAR?"??/"":""??>
main(argv, argc)
char * argc??(:>;
<%
    if (argv)
        printf("%s" "??/n", 0<:argc??));
??>
??=
```
